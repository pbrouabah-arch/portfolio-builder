import { scriptFont, serifFont } from "./fonts";
interface EducationProps { data: any; }
export default function Education({ data }: EducationProps) {
 const items = data.education ?? [];
 return <section id="education" className="bg-[#F6F0E4] px-8 py-32 text-[#46392E]"><div className="mx-auto max-w-7xl"><span className={`${scriptFont.className} text-2xl text-[#7C8264]`}>my academic path</span><h2 className={`${serifFont.className} mt-2 text-5xl font-semibold`}>Education</h2><p className="mt-5 max-w-3xl text-[#8B7B68]">Academic background and learning journey.</p>{items.length===0?<p className="mt-12 text-[#8B7B68]">No education added yet.</p>:<div className="mt-16 grid gap-8 md:grid-cols-2">{items.map((item:any)=><article key={item.id} className="rounded-sm border border-[#DCCBA8] bg-[#FFFDF8] p-8 shadow-[0_14px_30px_rgba(70,57,46,0.08)]"><h3 className="text-2xl font-bold">{item.degree || item.title || "Education"}</h3>{item.field_of_study&&<p className="mt-2 text-lg text-[#7C8264]">{item.field_of_study}</p>}{item.institution&&<p className="mt-2 text-[#8B7B68]">{item.institution}</p>}{(item.start_date||item.end_date||item.is_current)&&<p className="mt-4 text-sm uppercase tracking-wide text-[#7C8264]">{item.start_date||""} — {item.is_current?"Present":item.end_date||""}</p>}{item.description&&<p className="mt-5 leading-7 text-[#5E5142]">{item.description}</p>}</article>)}</div>}</div></section>;
}
