# Minimal template — visibility fix

These files keep the existing design and palette while making decorative background layers explicitly sit behind content.

Replace the matching files under `components/templates/minimal/`.

No data model or portfolio content was changed.
